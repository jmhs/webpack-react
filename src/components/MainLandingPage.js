import React, {PropTypes} from 'react';
import { connect } from 'react-redux';

export default class MainLandingPage extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
      <p>MainLandingPage Componcsent</p>
      </div>
    );
  }
}

MainLandingPage.propTypes = {
};
